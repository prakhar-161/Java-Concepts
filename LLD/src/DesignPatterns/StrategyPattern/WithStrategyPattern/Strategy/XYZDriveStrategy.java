package DesignPatterns.StrategyPattern.WithStrategyPattern.Strategy;

// concrete strategy
public class XYZDriveStrategy implements DriveStrategy{
    // In the future, if any new drive capability comes, we can just override the drive method

    // XYZ drive capability

    @Override
    public void drive() {

    }
}
